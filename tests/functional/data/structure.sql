CREATE TABLE article (id INTEGER NOT NULL, name VARCHAR(255) NOT NULL, PRIMARY KEY(id));
CREATE TABLE page (id INTEGER NOT NULL, article_id INTEGER DEFAULT NULL, PRIMARY KEY(id), CONSTRAINT FK_140AB6207294869C FOREIGN KEY (article_id) REFERENCES article (id) NOT DEFERRABLE INITIALLY IMMEDIATE);
CREATE UNIQUE INDEX UNIQ_140AB6207294869C ON page (article_id);
